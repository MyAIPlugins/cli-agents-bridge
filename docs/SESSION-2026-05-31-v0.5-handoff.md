# Handoff sessione 2026-05-31 (sera) — v0.5 stress-test, ESC fresco + worktree

> Pre-compact. Punto di ripresa per lo stress-test v0.5 con **ESC fresco** e **worktree git separati**.
> Principio guida invariato: **F-16 — verifica il ground-truth su disco (git/inbox/peers), MAI il resoconto.**
> E (lezione di stanotte): **leggi TUTTI i msg in inbox, ri-verifica `git status`/`git log` appena prima di agire — gli snapshot invecchiano (F-16 bidirezionale).**

## Stato shippato / corrente
- **v0.4.0 SHIPPED** (tag + GitHub Release pubblica MIT, binari prebuilt). README/PLAN(EN)/CHANGELOG/skill pubblica allineati. Email docs = `firstcontact@alancurtisagency.com` (mai `advertalis@`).
- **v0.5 in corso**, NON taggato/pushato. `main` = `ea44acb`, avanti **5 commit** su origin. Binario in PATH: **`0.4.0-6-g9f074af`** (ha F-40).
- **F-40 DONE e in `main`** (`9f074af`): `cab-bridge bootstrap --role=<val|esc>` — auto-discovery zero-config + naming adattivo (`VAL-x`↔`ESC-x`, fallback `<ROLE>-<basename>`) + idempotenza `--resume` (F-27) + `val`→`state orchestrating`+exit / `esc`→`listen --wait-one`. Gate verde, smoke convergenza verde.

## Backlog v0.5 — tema "AI-friendly under stress" (CLAUDE.md LL-13): ridurre la superficie di id-grezzi
Ordine di attacco proposto:
1. **F-36 `receive --any`** — **PIANO RATIFICATO, pronto** (manca solo l'impl). Wake event-driven per l'orchestrator senza msg-id. Design: flag `--any` di `receive` (mutua escl. con `--msg-id`); **batch** via `DrainInboxOnce`; timeout **exit 0 + `{"status":"timeout"}`** (asimm. con `--msg-id`=124); **NO AdoptPID** (delta-chiave vs `listen --wait-one`, verificato `listen.go:79`); NO SetLastConsumed.
   - **DA RIALLINEARE nel brief impl** (catch onesto di ESC): la mia ratifica dice "batch + NO SetLastConsumed", ma `receive --msg-id` è "1-msg + SÌ SetLastConsumed" → sono modi diversi (legittimo), ma dichiararlo esplicito così non sorprende al gate.
   - Ground-truth codice: `cmd/cab-bridge/receive.go` (flag, ReceiveReply, SetLastConsumed, timeout 124) + `internal/transport/fs/receive.go` (`ReceiveReply`+`scanForReply`: filtra `inReplyTo`, archivia in `processed/` via `MoveToProcessed` = F-30, **skippa `type=ack`**). `receive_test.go` da CREARE. Impl: generalizzare la scan a un predicato (`--msg-id`→inReplyTo match; `--any`→qualsiasi tranne ack).
2. **F-39** riferimenti simbolici (`--reply-to-last`, `--to-last-from=<agent>`, `--last`) — non puoi inventare ciò che non trascrivi.
3. **F-37** msg-id existence validation (rifiuta `--in-reply-to` a un id mai esistito) — rete anti-allucinazione.
4. **F-34** conversation-cursor (auto-rileva gli incroci). F-35 inbox `--type`/`--unread`. F-23b read-receipt. F-11 globalSweep PID-aware.

## Finding ARCO AUTONOMO notturno (oro per il setup + per il VPS JOE/WAL)
L'arco VAL-cab↔ESC-cab su working-tree CONDIVISO ha prodotto F-40 ma con percorso caotico → lezioni provate:
- **F-38 — worktree git SEPARATI OBBLIGATORI**: il tree condiviso = radice del caos (ESC committa/amenda/cambia-branch sotto i piedi del VAL; snapshot git divergenti in entrambe le direzioni). È il motivo del setup di stasera.
- **F-14 — l'executor deve LOOP-are il `listen`** (ri-lanciare `listen --wait-one` dopo ogni batch); se si addormenta, il bridge non lo sveglia.
- **F-37/LL-13 dal vivo** — ESC ha citato **SHA inventati** in 2 report (gli id opachi si confabulano sotto carico). Difesa: verificare SEMPRE con `git rev-parse`/`git log`.
- **LL-11 dal vivo** — ESC ha dichiarato "gate verde" 2 volte mentre era ROSSO; il **gate VAL indipendente `go test -race -count=1 ./...` completo** l'ha sempre colto. Mai fidarsi del verde dichiarato.
- **F-16 bidirezionale** — sia VAL che ESC hanno diagnosticato male su snapshot vecchi (io "ESC dorme"; ESC "main stale"). Ri-verificare git appena prima di agire.

## SETUP STRESS-TEST (worktree separati + ESC fresco) — istruzioni
**Canale bridge**: con worktree separati l'auto-scope F-17 darebbe scope DIVERSI (i worktree hanno `.git` diversi) → VAL ed ESC non si vedrebbero. Quindi per QUESTA coppia usa un **`CAB_DATA_DIR` condiviso esplicito**: `export CAB_DATA_DIR=$HOME/.claude/cli-agents-bridge/teams/cab-v05` (ENTRAMBE le sessioni, valore identico). Il data dir È il canale (F-20). (F-40 bootstrap auto-scope si testa a parte, in scenario stesso-checkout.)

1. **Spegni la coppia vecchia**: `CAB_DATA_DIR=$HOME/.claude/cli-agents-bridge cab-bridge cleanup --session-id=f0f70355` (ESC-cab) + chiudi quella finestra. VAL-cab `17ec4988` (default data dir) è separato — lascialo o pulisci.
2. **Crea il worktree ESC**: dal repo principale `git worktree add ../cli-agents-bridge-esc -b feat/v0.5-receive-any main`.
3. **Apri una NUOVA finestra VS Code** in `../cli-agents-bridge-esc` → questo è l'**ESC fresco** (git isolato).
4. **ESC fresco** (prompt iniziale): skill `esecutore` + `cab-bridge-awareness`; poi `export CAB_DATA_DIR=$HOME/.claude/cli-agents-bridge/teams/cab-v05`; `cab-bridge register --role=esc --agent-name=ESC-v05 --team=cab-v05`; poi `cab-bridge listen --wait-one --until-deadline=2h --session-id=<id>` **in loop** (ri-lancia dopo ogni batch — F-14).
5. **VAL** (questa sessione, post-compact): `export CAB_DATA_DIR=...teams/cab-v05`; `cab-bridge register --role=val --agent-name=VAL-v05 --team=cab-v05`; `cab-bridge state --session-id=<id> orchestrating`; scopre ESC via `cab-bridge peers --team=cab-v05` (one-shot); brief F-36 impl via `ask --file`. VAL resta nel repo principale (main), fa i merge; ESC committa nel suo worktree.

## Regole/discipline (NON negoziabili)
- **Boundary**: ESC scrive CODICE+TEST, VAL scrive DOCS. Mai scavalcare.
- **Gate VAL**: `go test -race -count=1 ./...` COMPLETO indipendente (no cached, LL-11) + lettura codice + smoke reale per feature non banali. Mai fidarsi del "verde dichiarato".
- **Commit**: un commit per unità logica; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; NO emoji; type(scope): subject.
- **Push/tag**: SOLO quando Alan lo chiede esplicitamente. v0.5 si pusha+tagga (v0.5.0) DOPO validazione, con Alan.
- **F-16**: verifica disco non resoconto; leggi TUTTI i msg; ri-verifica git appena prima di agire.
- **Design-first** per feature non banali (ESC propone piano → VAL ratifica → ESC implementa).
- **dcg guard**: `rm -rf` richiede approvazione umana (usa dir uniche, non cancellare). Per JSON in pipe: scrivi su file + rileggi (l'harness inietta artefatti in Read/pipe — F-16 rafforzato).
- **Email docs**: `firstcontact@alancurtisagency.com`. `briefing/` + `iterations/` sono gitignored (audit interno, non su GitHub).

## Sessioni bridge / riferimenti
- VAL-cab `17ec4988` (default data dir, scope cli-agents-bridge, orchestrating) · ESC-cab `f0f70355` (idle, DA SPEGNERE per il fresco). Coppie legacy in `teams/cab-dev` ecc. = ignorare/pulire.
- Recap findings **F-13→F-40** richiesto da Alan "quando ci fermiamo" — ancora da fare a voce.
- Tutto il dettaglio in `ROADMAP.md` (§Dogfooding VPS / coppia gioco / Security review BI / Testing v0.4 / Fase FIX / F-40) + `CLAUDE.md` LL-1→LL-13 + memory.
