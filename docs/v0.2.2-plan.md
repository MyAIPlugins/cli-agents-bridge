# v0.2.2 — Plan & Findings (kickoff per la prossima sessione)

> Documento self-contained: leggi SOLO questo per attaccare v0.2.2. Distillato dal doppio
> dogfooding del 2026-05-29 (2 progetti reali costruiti via cab-bridge). Dettaglio esteso nel
> backlog di `ROADMAP.md` (sezioni "Dogfooding findings"). Findings = uso reale, non speculazione.

---

## Dove siamo

- **Repo**: `github.com/MyAIPlugins/cli-agents-bridge` (public MIT). Branch `main`.
- **Rilasciati**: `v0.2.0` (MVP: 9 bug Patil fixati, Go single binary, self-marketplace) + `v0.2.1` (auto-gc orphan sessions + AUDIT-1 data-loss fix §1.6 + F-A/F-B). Entrambi tag + GitHub Release.
- **Binario**: `~/.local/bin/cab-bridge` = 0.2.1 (via `make install-dev`). Plugin cache 0.2.1.
- **Distribuzione**: source-first (binari multi-OS GoReleaser ancora da fare — vedi sotto).
- **Skill operativa**: `~/.claude/skills/cab-bridge-awareness/SKILL.md` — già raffinata coi findings di stasera (wake, identità, ACK, recovery). NON è ancora distribuita col plugin (backlog).

## Cosa NON toccare (base solida, confermata da entrambe le coppie)

Zero data-loss (git+inbox preservati sempre), `ask --file` impeccabile sui payload lunghi, disciplina "verifica-non-assumere" (git+inbox = ground-truth). **v0.2.2 è tutto OSSERVABILITÀ + REATTIVITÀ + ISOLAMENTO — niente sull'affidabilità dei dati.**

---

## I 13 findings (F-8 ritirato) — prioritizzati per v0.2.2

### MUST #1 — F-12: osservabilità dello stato-task (ACK) — IMPLEMENTATO 2026-05-30
**STATO**: completo su `feat/v0.2.2` (NON pushato). 4 commit: `9e0c6c6` ack type + lenient forward-compat, `f565985` lastConsumedMsgId + manifest mutex, `1605835` auto-ack + scanForReply skip-ack + `--no-auto-ack`, `523fb32` inboxCount + lastConsumedMsgId in peers/status. Gate VAL `go test -race -count=1 ./...` VERDE 10/10 indipendente (nessun cached, LL-11) + lettura riga-per-riga delle parti critiche OK.
**5 ratifiche di design (VAL)**: (A) forward-compat solo `type`, `status` resta strict ovunque — `validateCommon` (status strict) vs `Validate` (type strict); (B) opt-out `listen --no-auto-ack` flag-only, niente config bool (YAGNI); (C) `scanForReply` salta `type==ack` — l'ack resta in inbox come segnale F-12, receive aspetta la vera response; (D) mutex process-local su OGNI RMW del manifest (touchHeartbeat/AdoptPID/SetLastConsumed), non solo le 2 in contesa; (E) naming `inboxCount` ovunque (no `inboxPending`, coerenza col codice esistente). Anti-loop: allow-list `{query}` — l'ack è `type=ack`, mai query, loop strutturalmente impossibile. Ruoli letti dai manifest (non dal messaggio in arrivo, che porta la vista invertita del mittente).
**Problema**: al timeout di `receive` l'orchestrator non distingue 3 casi — ESC lavora-lentamente / non ha preso il brief / ha finito ma reply persa. `heartbeat`/`stale:false` dice solo che il processo listen è vivo, NON che l'agente stia agendo. → escalation a vuoto ("stallo pnpm", zero dati persi, solo percezione).
**Fix prodotto**:
- `--type=ack` dedicato nell'enum message (oggi si usa `--type=notify` come convenzione — già nella skill).
- **auto-ack**: il binario, quando `listen` emette un brief, invia automaticamente un ACK al mittente ("ricevuto"). Così l'orchestrator ha la macchina a stati `inviato → ack → done` senza disciplina manuale.
- `peers`/`status` espongano `inboxPending` (n. messaggi non consumati) + `lastConsumedMsgId` per sessione → distingue idle-vs-working senza ACK.
**File**: `internal/message/schema.go` (enum), `cmd/cab-bridge/listen.go` (auto-ack su emit), `cmd/cab-bridge/peers.go`+`status.go` (campi). **Test**: ESC riceve brief → ACK auto inviato; peers mostra pending/consumed corretti.

### MUST #2 — F-10: wake non immediato — IMPLEMENTATO 2026-05-30
**STATO**: completo su `feat/v0.2.2` (commit `f52addc`). `listen --wait-one` esce (exit 0) dopo il primo sweep NON-VUOTO via nuova `DrainInboxOnce` (sincrona, zero-perdita PER COSTRUZIONE — non perde i messaggi che `PollInbox` pre-sposta in `processed/` prima dell'emit bloccante). Helper `consumeInboxEntry` DRY condiviso con `emitInboxOnce` (`poll_test.go` invariato). Fix teardown: `cancel()` esplicito prima del return nel ramo wait-one (deadlock LIFO `<-hbDone`-prima-di-`cancel` evitato — catch ESC oltre il brief). Exit 0 (batch) / 124 (timeout) / 0 (SIGINT), default `PollInbox` invariato. Gate VAL `-race -count=1` VERDE 10/10 indipendente + lettura riga-per-riga. NON pushato.
**Problema**: `cab-bridge listen` in Bash `run_in_background` notifica l'agente SOLO all'EXIT del comando (timeout 124), NON all'arrivo del messaggio. Con finestra lunga (1800s) i messaggi urgenti restano invisibili fino a 30min → dipendenza da ping umano. **(Mea culpa VAL: la skill v0.2.1 raccomandava finestra-lunga — peggiorava. Ora corretta: Monitor o finestra breve.)**
**Fix prodotto**:
- `cab listen --wait-one`: esce al PRIMO messaggio ricevuto → `run_in_background` notifica all'arrivo (wake immediato) → l'agente processa e ri-lancia. Pattern naturale per il background del harness.
- Documentare l'uso del tool **Monitor** (notifica per riga stdout) come alternativa per wake event-driven reale.
**File**: `cmd/cab-bridge/listen.go` (flag `--wait-one` / exit-on-first-message). **Test**: listen --wait-one esce appena arriva 1 msg; integrazione subprocess.

### MUST #3 — F-5: isolamento coppia/team — IMPLEMENTATO 2026-05-30 (+ smoke PASS)
**STATO**: completo su `feat/v0.2.2` (commit `54d4e36`). `teamId,omitempty` nel manifest (additivo, retro-compat) + `register --team` (validato da `security.ValidateTeamID` regex `^[a-z0-9][a-z0-9_-]{0,31}$`, solo se non-vuoto) + `peers --team=<x>` filtro (default globale invariato) + colonna TEAM in table + nuovo `cab whoami` (sessionId/agent/role/teamId/projectPath COMPLETO/**dataDir**). F-6 risolto da whoami (projectPath completo), niente fix a projectName. Gate VAL `-race -count=1` verde indipendente + lettura riga-per-riga.
**SMOKE REALE PASS** (binario installato, data dir isolato): peers colonna TEAM + `--team` filtra + whoami (projectPath completo + dataDir) + `--team='Bad Name'` exit 1 + auto-ack reale (listen consuma query → ACK con inReplyTo) + `lastConsumedMsgId`/`inboxCount` in peers + `listen --wait-one` exit 0. Tutti e 3 i MUST verdi nel modello d'uso (LL-10).
**Problema**: data dir globale condiviso → `peers` mostra le sessioni di TUTTI i progetti → confusione identità (entrambe le coppie hanno scambiato le sessioni dell'altra per le proprie). Aggravato da F-6 (`projectName=basename(cwd)` → due `src/` collidono).
**Fix immediato (zero codice, già documentato in skill)**: un `CAB_DATA_DIR=~/.claude/cli-agents-bridge/teams/<progetto>` per coppia.
**Fix prodotto v0.2.2/v0.3**:
- reintrodurre `teamId` nel manifest (era tagliato in FIX-2 come YAGNI — il caso multi-coppia §1.4 è arrivato) + `cab register --team=<nome>` + `peers` filtra per team di default.
- `cab whoami`: mostra la sessione corrente per `projectPath` COMPLETO (non basename) → l'agente identifica se stesso senza ambiguità.
**File**: `internal/session/schema.go` (manifest +teamId), `cmd/cab-bridge/register.go` (--team), `peers.go` (filtro), nuovo `whoami.go`. **Test**: 2 coppie stesso data dir, team diversi → peers --team mostra solo la propria.

### Poi (stessa release o v0.2.3, valutare):
- **F-9 — IMPLEMENTATO 2026-05-30** (commit `3a69ca3`): `sendMessage` scrive una copia best-effort nell'outbox del mittente (copre `ask` + auto-ack, non-fatale, msgID ritornato comunque) + nuovo `cab sent` che lista l'outbox proprio (MsgID/To/Type/Timestamp/InReplyTo). `outboxCount` ora significativo. Gate verde + test (`OutboxCopyNonFatal` guard, `scenario_outbox`). **v0.2.2 codice COMPLETO**: 8 commit su `feat/v0.2.2` (F-12×4 + F-10 `f52addc` + F-5 `54d4e36` + F-9 `3a69ca3` + release `8b5a751` bump→0.2.2). Gate `-race -count=1` verde indipendente, smoke reale PASS, version 0.2.2, lettura test critici fatta. PRE-TAG residuo: SECURITY.md SC-3 ref + lettura unit puri.
- **F-11**: race cleanup/GC vs delivery (un `ask` con msg-id ritornato ma file sparito per cleanup concorrente). Mitigato da F-5. Fix: grace-period o lock su sessioni che ricevono. **File**: `internal/cleanup/gc.go` + `scope.go`.
- **F-6**: `projectName=basename` collisione — risolto di fatto da F-5 (whoami con path completo + team). Non serve fix isolato se F-5 fatto.
- **F-13**: orfani al lifecycle delle finestre (dogfooding 2026-05-30). Chiudendo una finestra VS Code le sessioni di quella coppia restano STALE fino all'auto-gc (PID morto + heartbeat >`AutoGCHours`=24h) → su più progetti aperti/chiusi si accumulano in `peers`. **L'hook "alla chiusura" NON è fattibile** (verificato su doc ufficiale Claude Code via claude-code-guide): `SessionEnd` scatta solo su `/clear`/`logout`, NON su window-close / Quit / force-quit / crash; nessun hook di shutdown è garantito per natura. Pattern robusto = riconciliazione all'AVVIO, non cleanup alla chiusura.
  **Fix prodotto**: (a) `cab cleanup --dead` — alias semantico che rimuove ogni sessione con **PID morto** ignorando la soglia temporale (più intuitivo di `--scope=global`, che ragiona sul tempo); (b) **hardening** `globalSweep`: deve rispettare anche il **PID vivo** — oggi (`scope.go:117`) si fida SOLO dell'heartbeat (NESSUN check `IsProcessAlive`, a differenza di `GCOrphans` in `gc.go:79`) → può rimuovere una sessione viva non-in-listen da >5min; fragilità che si lega a F-11; (c) template hook `SessionStart` (riconciliazione deterministica all'apertura, vale anche per terminale diretto) + `SessionEnd` best-effort, shippati col plugin + già documentati nella skill. **File**: `internal/cleanup/scope.go` (globalSweep +PID-alive check), `cmd/cab-bridge/cleanup.go` (flag `--dead`), nuovo `hooks/` template + skill `cab-bridge-awareness`. **Workaround ORA (zero codice)**: `cab-bridge cleanup --scope=global --force` (o `cleanup --session-id=<id>` chirurgico). **Test**: globalSweep NON rimuove una sessione con PID vivo anche se heartbeat stale; `--dead` rimuove solo PID-morti a prescindere dall'età.

### Finding di metodo dal dogfooding di F-12 (2026-05-30, coppia VAL-bridge/ESC-bridge)
Emersi implementando F-12 stesso. Sono lezioni di workflow VAL/ESC (skill) più qualche angolo prodotto:
- **F-14 — executor SORDO durante l'implementazione**: mentre ESC implementa (fuori `listen`, testa-bassa), i messaggi urgenti restano in inbox NON letti finché non rientra in ascolto. Stasera un mio STOP/correzione è stato letto solo DOPO i 4 commit (per fortuna il codice era già giusto). Mitigazione workflow (skill): l'executor polla l'inbox a checkpoint tra un commit e l'altro. Angolo prodotto da valutare: un segnale/exit "messaggio urgente pending". Correlato F-10.
- **F-15 — ACK semantico (riassunto) sciatto → falso allarme orchestrator**: l'ACK manuale di ESC riassumeva 2 ratifiche al contrario (status simmetrico/inboxPending) + id allucinato, pur avendo implementato il codice CORRETTAMENTE. Mi ha indotto una correzione ridondante (rework inutile). Rafforza il valore dell'auto-ack minimale (generato dal binario, zero interpretazione): per un ACK manuale "ricevuto msg-X" basta; il riassunto solo se accurato. La verità è git/codice, non l'ACK.
- **F-16 (lezione madre) — degrado/allucinazione dell'executor dopo sessione lunga**: dopo ~2h ESC ha prodotto 4 allucinazioni (id inventato, un "FINDING-1" mai esistito, un campo schema `content_truncated_note` impossibile, un "gate 10/10" che il VAL non aveva fatto). Codice impeccabile, resoconto inaffidabile. **L'orchestrator DEVE verificare il ground-truth (git + file su disco), MAI fidarsi del resoconto dell'executor.** Stasera questo ha evitato di inventare lavoro su un'allucinazione. Mitigazione: sessioni executor brevi / un task per sessione / reset frequenti; gate e stato verificati sempre dal VAL su disco.

### Pre-tag v0.2.2 (item bloccanti per il tag)
- **Version bump 0.2.1 → 0.2.2**: la version string del binario stampa ancora `0.2.1` (`built: bin/cab-bridge (0.2.1)`). Bumpare la costante version + `plugin.json` + `marketplace.json` prima del tag.
- **Lettura completa dei test** dei 3 MUST (al tag, come per v0.2.0 LL-9): finora gate verde + lettura codice produzione; i file di test (drain_test, scenario_waitone_test, perms_test, scenario_team_test) vanno riletti riga-per-riga pre-release pubblica.

### Distribution + hardening deferred (da v0.2.0/v0.2.1, ancora aperti)
- **GoReleaser**: `.github/workflows/release.yml` per pubblicare binari multi-OS (darwin-arm64/amd64, linux-amd64/arm64) sulle GitHub Release → chiude il "source-first". + brew tap.
- **SC-3 ownership wiring**: `CheckOwnership` dentro `LoadManifest` via `fstat`-su-fd (chiude anche Stat-vs-Lstat TOCTOU). Oggi SC-3 è dichiarato "deferred v0.2.1" in SECURITY.md.
- **os.Root** symlink-safe per creazione sessioni/inbox (Go 1.25+).
- **inbox backpressure bound**: `MaxInboxSize` è dead config — enforcing count-before-write.
- **scanForReply idempotente** su already-consumed (receive.go).

---

## Workflow per attaccare v0.2.2

1. **VAL-bridge** (sessione in `/Users/alan/develop/cli-agents-bridge`) = io, orchestrator/gate.
2. **ESC** in cwd disgiunta (subdir o worktree), carica `/esecutore` + `/cab-bridge-awareness`.
3. **Trasporto**: cab-bridge stesso (dogfooding continua), con `CAB_DATA_DIR=~/.claude/cli-agents-bridge/teams/cab-dev` ISOLATO (lezione F-5). Pattern ACK fin da subito (skill).
4. **Soglia autonomia VAL** (lezione dogfooding): ESC procede su eng/fix/docs; go umano solo per scelte di design/scope. Niente gate per-fase a vuoto.
5. **No Day-0 spike** (architettura nota, plugin/marketplace già validati). Si parte diretti dal primo MUST.
6. **Gate VAL** (LL-9/11): rilancia SEMPRE `go test -race -count=1 ./...` COMPLETO indipendente — mai fidarsi del "verde dichiarato" (catturato 2 volte). Per security/release: lettura codice riga-per-riga + smoke reale.

## Lezioni di metodo (dal doppio dogfooding)

- **2 mei culpa VAL**: F-8 (promosso "azione non eseguita" su snapshot prematuro + ignoravo che `ask` non popola outbox) e F-10 (skill raccomandava finestra-lunga per il wake — sbagliato). Entrambi = **assunzioni non verificate sul MODELLO del bridge**. Regola: verifica empiricamente il comportamento del bridge prima di asserirlo, vale anche per il VALUTATORE.
- Il dogfooding crescente rivela i limiti a strati: coppia singola breve (auto-gc) → niente; videogame lungo → F-1/F-2; seconda coppia parallela → F-5/F-6/F-7; task post-conclusione → F-12. Ogni stress nuovo scopre il prossimo.
- Costo dei problemi su 2 progetti reali: **zero rework sul codice**. Le frizioni costano comunicazione + attesa, non correttezza.

## Stato progetti dogfooding (riferimento)
- **ac-flusso-perfetto** (videogame "Flusso Perfetto", Infordata/TCA): 6/6 fasi, consegnabile, tag v1. Manca solo deploy/hosting (CORS iframe).
- **ac-links**: bug prod URIError risolto, 4 PR in prod, 43/43 test, migrazione dati applicata.
