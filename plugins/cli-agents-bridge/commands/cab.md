---
description: cli-agents-bridge multiplexer — show help and available subcommands
---

Invoke the cab-bridge binary help:

!`cab-bridge --help`

cab-bridge bridges CLI agent sessions (Claude Code, Codex, Aider, ...) over filesystem IPC. Subcommands: register, listen, ask, connect, receive, peers, cleanup, status, inspect, migrate-from-patil. Each prints its own --help.
