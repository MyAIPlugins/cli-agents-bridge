.DS_Store
*.log
.env
.env.local
node_modules/
__pycache__/
*.pyc
.venv/
venv/
.idea/
.vscode/
*.swp
*.tmp
coverage/
dist/
build/
upstream/
.tmp/

# Go
bin/
*.test
*.out
*.prof
vendor/
.coverage.out

# Bridge session artifacts (session-bridge plugin scratch, not project state)
.claude/
docs/.claude/

# Serena MCP local index/cache — machine-local, never project state
.serena/

# Internal VAL/ESC reports & archived planning — not for publication
briefing/
iterations/
feedback/

# Handoff di sessione: stato di lavoro con vita di ore, non contenuto del progetto.
# Il corrente sta in .handover/, i precedenti in .handover/archive/.
.handover/

# Git worktrees for VAL/ESC pairs (separate checkouts under the repo, never tracked).
# Dotted name: go ./... and recursive search ignore it regardless; gitignore keeps git clean.
.worktrees/

# Artefatti dei verificatori empirici (screenshot, trace, profili del Playwright MCP).
# Nascono dentro il repo perche il MCP tiene il browser nella propria root: si
# ignorano, non si spostano.
.playwright-mcp/
*-proof.png

# Note di metodo interne: caricate come ISTRUZIONI da Claude Code in
# qualsiasi clone, quindi non vanno nel repo pubblico. Vedi F-127.
CLAUDE.md
